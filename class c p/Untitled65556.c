#include <stdio.h>
int main()
{
    int c, n, s, rows;
    printf("Enter number of rows: ");
    scanf("%d",&n);
    s=n
    for(row=1;row<=n;++row)
    {
    	for(c=1;c<s;c++)
    	printf(" ");
    	s--;
	}
